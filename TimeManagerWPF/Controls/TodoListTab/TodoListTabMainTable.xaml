<UserControl x:Class="TodoList.WPF.Controls.TodoListTabMainTable"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:beh="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:vm="clr-namespace:TodoList.WPF.ViewModels"
             xmlns:icons="http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=vm:TodoListTabViewModel, IsDesignTimeCreatable=False}"
             d:DesignHeight="200" d:DesignWidth="800">
    <DataGrid ItemsSource="{Binding FilteredTodoItems}" 
                  SelectedValue="{Binding SelectedJobItem}"   
                  FontSize="15"
                  CanUserAddRows="False"
                  CanUserSortColumns="False"
                  CanUserReorderColumns="False"
                  CanUserResizeRows="False"
                  CanUserDeleteRows="False"
                  BorderThickness="0"
                  VerticalGridLinesBrush="Transparent"
                  Background="Transparent"
                  RowBackground="Transparent"
                  HeadersVisibility="Column"
                  HorizontalGridLinesBrush="LightGray"
                  RowHeight="28"
                  SelectionMode="Single"
                  AutoGenerateColumns="False">
        <beh:Interaction.Triggers>
            <beh:KeyTrigger Key="Delete">
                <beh:InvokeCommandAction Command="{Binding DeleteJobCommand}" />
            </beh:KeyTrigger>
        </beh:Interaction.Triggers>
        <DataGrid.Resources>
            <Style TargetType="{x:Type DataGridCell}">
                <Setter Property="Foreground" Value="Black" />
                <Style.Triggers>
                    <Trigger Property="IsSelected" Value="True">
                        <Setter Property="Background" Value="#33ffff00" />
                        <Setter Property="BorderBrush" Value="{x:Null}" />
                    </Trigger>
                </Style.Triggers>
            </Style>
        </DataGrid.Resources>
        <DataGrid.RowStyle>
            <Style TargetType="DataGridRow">
                <Style.Triggers>
                    <DataTrigger Binding="{Binding IsCompleted}" Value="True">
                        <Setter Property="Foreground" Value="Black"></Setter>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding IsCompleted}" Value="false">
                        <Setter Property="Foreground" Value="Navy"></Setter>
                        <Setter Property="FontWeight" Value="Bold"></Setter>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </DataGrid.RowStyle>
        <DataGrid.Columns>
            <DataGridTemplateColumn Header="Дата">
                <DataGridTemplateColumn.CellTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding StartDate, StringFormat='dd.MM.yyyy'}" />
                    </DataTemplate>
                </DataGridTemplateColumn.CellTemplate>
                <DataGridTemplateColumn.CellEditingTemplate>
                    <DataTemplate>
                        <DatePicker SelectedDate="{Binding StartDate, UpdateSourceTrigger=PropertyChanged}" BorderThickness="0" Background="Transparent" Foreground="Blue"/>
                    </DataTemplate>
                </DataGridTemplateColumn.CellEditingTemplate>
            </DataGridTemplateColumn>
            <DataGridTextColumn Header="Задача" Width="2*" Binding="{Binding Title,Mode=TwoWay, UpdateSourceTrigger=LostFocus}"/>
            <DataGridTextColumn Header="Сайт" Binding="{Binding Website, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />
            <DataGridTextColumn Header="Цена" Binding="{Binding Price, Mode=TwoWay, UpdateSourceTrigger=LostFocus}"/>
            <DataGridCheckBoxColumn Header="Оплачено" Binding="{Binding IsPayed, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <DataGridCheckBoxColumn Header="Выполнено" Binding="{Binding IsCompleted, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <DataGridTemplateColumn Header="img">
                <DataGridTemplateColumn.CellTemplate>
                    <DataTemplate>
                        <Button Style="{StaticResource ToolButtonStyle}"
                                        Padding="2"
                                                    Command="{Binding OpenScreenshotsFolderCommand}"
                                                    Foreground="{Binding HasScreenshots, Converter={StaticResource HasActiveTaskBrushConverter}}" 
                                                    HorizontalAlignment="Center"
                                                    VerticalAlignment="Center">
                            <icons:PackIconFontAwesome Kind="ImageRegular" Width="16" Height="16"/>
                        </Button>
                    </DataTemplate>
                </DataGridTemplateColumn.CellTemplate>
            </DataGridTemplateColumn>
            <DataGridTextColumn Header="Примечание" Width="*" Binding="{Binding Description, Mode=TwoWay, UpdateSourceTrigger=LostFocus}"/>
        </DataGrid.Columns>
    </DataGrid>
</UserControl>
