<UserControl x:Class="TodoList.WPF.Views.ConnectionErrorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:i="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:vm="clr-namespace:TodoList.WPF.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:ConnectionErrorViewModel, IsDesignTimeCreatable=True}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Ошибка соединения"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   FontSize="16"
                   Foreground="Orange"
                   />
        <i:PackIconFontAwesome Kind="ExclamationTriangleSolid" Grid.Row="1" 
                               HorizontalContentAlignment="Center"
                               VerticalContentAlignment="Center"
                               Width="200"
                               Height="200"
                               Foreground="Red"
                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <TextBlock d:Text="Ошибка соединения с базой данных ..."
                   Text="{Binding ErrorMessage}"
                   Grid.Row="2"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   FontSize="14"
                   Foreground="Orange"
                   />
    </Grid>
</UserControl>
