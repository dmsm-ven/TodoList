<UserControl x:Class="TodoList.WPF.Views.AddEmployeerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:icons="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:vm="clr-namespace:TodoList.WPF.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=vm:AddEmployeerViewModel, IsDesignTimeCreatable=False}"
             d:DesignHeight="200" d:DesignWidth="300" d:Background="White">
    <Border BorderBrush="LightGray" CornerRadius="4" 
            BorderThickness="1" Margin="4" 
            HorizontalAlignment="Center" VerticalAlignment="Center">

        <Border.Resources>
            <Style TargetType="Label">
                <Setter Property="Foreground" Value="Black"/>
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="BorderBrush" Value="LightGray"/>
                <Setter Property="Margin" Value="4 4 4 12" />
            </Style>
            <Style TargetType="TextBox">
                <Setter Property="Foreground" Value="Navy"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="BorderBrush" Value="LightGray"/>
                <Setter Property="Margin" Value="4 4 4 12" />
                <Setter Property="BorderThickness" Value="0 0 0 1"/>
            </Style>
        </Border.Resources>
        
        <StackPanel Margin="20 20" MinWidth="300" MinHeight="130">

            <Label Content="ФИО/Название работодателя" HorizontalContentAlignment="Center" />
            <TextBox Text="{Binding NewEmployeer.Name, UpdateSourceTrigger=PropertyChanged}" 
                     HorizontalContentAlignment="Center" />

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="4">
                <Button Style="{StaticResource AddEmployeerButton}" Command="{Binding CreateCommand}">
                    <StackPanel Orientation="Horizontal">
                        <icons:PackIconFontAwesome Kind="UserPlusSolid" />
                        <TextBlock Text="Добавить" Margin="16 2 8 2" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

            </StackPanel>

        </StackPanel>
    </Border>
</UserControl>
