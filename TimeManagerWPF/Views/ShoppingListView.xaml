<UserControl x:Class="TodoList.WPF.Views.ShoppingListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:TodoList.WPF.ViewModels"
             xmlns:icons="http://metro.mahapps.com/winfx/xaml/iconpacks"
             d:DataContext="{d:DesignInstance Type=vm:ShoppingListViewModel, IsDesignTimeCreatable=False}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Список покупок" />
        <Button Command="{Binding AddNewItemCommand}" Style="{StaticResource ToolButtonStyle}">
            <icons:PackIconFontAwesome Kind="PlusCircleSolid" Width="32" Height="32" Foreground="Beige" />
        </Button>
        <Border Grid.Row="2">
            <DataGrid ItemsSource="{Binding Items}" />
        </Border>

    </Grid>
</UserControl>
