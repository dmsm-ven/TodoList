<Window x:Class="TodoList.WPF.Views.ConnectionErrorWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:i="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:vm="clr-namespace:TodoList.WPF.ViewModels"
             MouseLeftButtonDown="Window_MouseLeftButtonDown"
             d:DataContext="{d:DesignInstance Type=vm:ConnectionErrorWindowViewModel, IsDesignTimeCreatable=True}"
             mc:Ignorable="d" 
            AllowsTransparency="True"
            Background="Transparent"           
            WindowStartupLocation="CenterScreen"
            WindowStyle="None" Width="400" Height="600">
    <Border CornerRadius="12" BorderThickness="2" BorderBrush="#A9AE90" Padding="6" Background="#B9211C1C">

        <Grid>
            <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Ошибка"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   FontSize="16"
                   Foreground="Orange"
                   />
        <i:PackIconFontAwesome Kind="ExclamationTriangleSolid" Grid.Row="1" 
                               HorizontalContentAlignment="Center"
                               VerticalContentAlignment="Center"
                               Width="200"
                               Height="200"
                               Foreground="Red"
                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <TextBlock d:Text="Ошибка соединения с базой данных ..."
                   Text="{Binding ErrorMessage}"
                   Grid.Row="2"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   FontSize="14"
                   Foreground="Orange"
                   />
        <Button Command="{Binding CloseApplicationCommand}" Grid.Row="0" HorizontalAlignment="Right"
                Style="{StaticResource ToolButtonStyle}">
            <i:PackIconFontAwesome Kind="TimesSolid" Width="40" Height="40"/>
        </Button>
    </Grid>
    </Border>
</Window>
