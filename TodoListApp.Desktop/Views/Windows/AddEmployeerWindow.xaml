<Window
    x:Class="TodoList.WPF.Views.AddEmployeerWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:icons="http://metro.mahapps.com/winfx/xaml/iconpacks"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:TodoList.WPF.ViewModels.Windows"
    x:Name="window"
    Width="500"
    Height="300"
    d:Background="White"
    d:DataContext="{d:DesignInstance Type=vm:AddEmployeerWindowViewModel,
                                     IsDesignTimeCreatable=False}"
    d:DesignHeight="200"
    d:DesignWidth="300"
    ResizeMode="NoResize"
    SizeToContent="WidthAndHeight"
    WindowStartupLocation="CenterScreen"
    WindowStyle="ToolWindow"
    mc:Ignorable="d">
    <Border
        Margin="4"
        HorizontalAlignment="Center"
        VerticalAlignment="Center"
        BorderBrush="LightGray"
        BorderThickness="1"
        CornerRadius="4">

        <Border.Resources>
            <Style TargetType="Label">
                <Setter Property="Foreground" Value="Black" />
                <Setter Property="FontSize" Value="12" />
                <Setter Property="BorderBrush" Value="LightGray" />
                <Setter Property="Margin" Value="4,4,4,12" />
            </Style>
            <Style TargetType="TextBox">
                <Setter Property="Foreground" Value="Navy" />
                <Setter Property="FontWeight" Value="Bold" />
                <Setter Property="FontSize" Value="14" />
                <Setter Property="BorderBrush" Value="LightGray" />
                <Setter Property="Margin" Value="4,4,4,12" />
                <Setter Property="BorderThickness" Value="0,0,0,1" />
            </Style>
        </Border.Resources>

        <StackPanel
            MinWidth="300"
            MinHeight="130"
            Margin="20,20">

            <Label HorizontalContentAlignment="Center" Content="ФИО/Название работодателя" />
            <TextBox HorizontalContentAlignment="Center" Text="{Binding NewEmployeer.Name, UpdateSourceTrigger=PropertyChanged}" />

            <StackPanel
                Margin="4"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Orientation="Horizontal">
                <Button
                    Command="{Binding CreateCommand}"
                    CommandParameter="{Binding ElementName=window}"
                    Style="{StaticResource AddEmployeerButton}">
                    <StackPanel Orientation="Horizontal">
                        <icons:PackIconFontAwesome Kind="UserPlusSolid" />
                        <TextBlock
                            Margin="16,2,8,2"
                            VerticalAlignment="Center"
                            Text="Добавить" />
                    </StackPanel>
                </Button>

            </StackPanel>

        </StackPanel>
    </Border>
</Window>
