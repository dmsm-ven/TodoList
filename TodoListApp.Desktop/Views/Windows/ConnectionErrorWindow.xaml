<Window
    x:Class="TodoListApp.Desktop.Views.ConnectionErrorWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i="http://metro.mahapps.com/winfx/xaml/iconpacks"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:TodoListApp.Desktop.ViewModels.Windows"
    Width="400"
    Height="600"
    d:DataContext="{d:DesignInstance Type=vm:ConnectionErrorWindowViewModel,
                                     IsDesignTimeCreatable=True}"
    AllowsTransparency="True"
    Background="Transparent"
    MouseLeftButtonDown="Window_MouseLeftButtonDown"
    WindowStartupLocation="CenterScreen"
    WindowStyle="None"
    mc:Ignorable="d">
    <Border
        Padding="6"
        Background="#B9211C1C"
        BorderBrush="#A9AE90"
        BorderThickness="2"
        CornerRadius="12">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBlock
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                FontSize="16"
                Foreground="Orange"
                Text="Ошибка" />
            <i:PackIconFontAwesome
                Grid.Row="1"
                Width="200"
                Height="200"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                HorizontalContentAlignment="Center"
                VerticalContentAlignment="Center"
                Foreground="Red"
                Kind="ExclamationTriangleSolid" />
            <TextBlock
                Grid.Row="2"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                d:Text="Ошибка соединения с базой данных ..."
                FontSize="14"
                Foreground="Orange"
                Text="{Binding ErrorMessage}" />
            <Button
                Grid.Row="0"
                HorizontalAlignment="Right"
                Command="{Binding CloseApplicationCommand}"
                Style="{StaticResource ToolButtonStyle}">
                <i:PackIconFontAwesome
                    Width="40"
                    Height="40"
                    Kind="TimesSolid" />
            </Button>
        </Grid>
    </Border>
</Window>
