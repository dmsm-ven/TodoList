<UserControl
    x:Class="TodoListApp.Desktop.Views.Controls.TodoListTabStatusBar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:TodoListApp.Desktop.ViewModels"
    d:DataContext="{d:DesignInstance Type=vm:TodoListTabStatusBarViewModel,
                                     IsDesignTimeCreatable=True}"
    d:DesignHeight="40"
    d:DesignWidth="600"
    mc:Ignorable="d">
    <StatusBar Margin="0,2,0,0">
        <StatusBarItem Content="{Binding ActiveTasksMessage}" />
        <Separator />

        <StatusBarItem Content="{Binding TotalWorkCashMessage}" />
        <Separator />

        <StatusBarItem Content="{Binding AlreadyPayedTasksCashMessage}" />
        <Separator />

        <StatusBarItem Content="{Binding NotPayedTasksCashMessage}" />
    </StatusBar>
</UserControl>
